[Unit]
Description=Claude Code token refresh (keeps ~/.claude/.credentials.json valid)

[Service]
Type=oneshot
Environment=PATH=%h/opt/node/bin:%h/.local/bin:/usr/local/bin:/usr/bin:/bin
ExecStart=/bin/sh -c 'claude -p "responda apenas: ok"'
